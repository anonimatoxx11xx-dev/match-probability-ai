{
  "name": "match-probability-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute match-probability-ai-db --local --file=schema.sql",
    "db:seed": "wrangler d1 execute match-probability-ai-db --local --file=seed.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.128.0"
  }
}

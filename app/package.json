{
  "name": "match-probability-ai-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "android:sync": "npm run build && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0",
    "@capacitor/status-bar": "^7.0.0",
    "vite": "^7.1.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}

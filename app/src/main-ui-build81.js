/* BUILD 85 interactive UI layer. Build78 engine/data logic remains untouched. */
import './main-ui-build80.js';
import './style-build81.css';
import './style-build85.css';

const appRoot = () => document.querySelector('#app');
let toastTimer = null;
const crestCache = new Map();
function toast(message) { let element=document.querySelector('.b81-toast'); if(!element){element=document.createElement('div');element.className='b81-toast';document.body.appendChild(element);} element.textContent=message;element.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>element.classList.remove('show'),1700); }
function closeOverlay(){document.querySelector('.b81-overlay')?.remove();}
function openSearch(){closeOverlay();const overlay=document.createElement('div');overlay.className='b81-overlay';overlay.innerHTML=`<div class="b81-modal"><div class="b81-modal-head"><b>🔎 Cerca partite</b><button class="b81-close" type="button">×</button></div><input class="b81-search" autocomplete="off" placeholder="Cerca squadra o campionato…"><div class="b81-result">Scrivi per filtrare le partite di oggi.</div></div>`;document.body.appendChild(overlay);const input=overlay.querySelector('.b81-search'),result=overlay.querySelector('.b81-result');const filter=()=>{const query=input.value.trim().toLowerCase();let visible=0;document.querySelectorAll('.match-card').forEach(card=>{const match=!query||card.textContent.toLowerCase().includes(query);card.classList.toggle('b81-hidden',!match);if(match)visible++;});result.innerHTML=query?`<b>${visible}</b> partite trovate per “${query}”.`:'Scrivi per filtrare le partite di oggi.';};input.addEventListener('input',filter);overlay.querySelector('.b81-close').addEventListener('click',closeOverlay);overlay.addEventListener('click',e=>{if(e.target===overlay)closeOverlay();});setTimeout(()=>input.focus(),40);}
function openSettings(){closeOverlay();const compact=localStorage.getItem('mp_compact')==='1',overlay=document.createElement('div');overlay.className='b81-overlay';overlay.innerHTML=`<div class="b81-modal"><div class="b81-modal-head"><b>⚙ Impostazioni vista</b><button class="b81-close" type="button">×</button></div><div class="b81-setting"><span><b>Vista compatta</b><br><small>Più partite visibili sullo schermo</small></span><button class="b81-toggle ${compact?'on':''}" data-compact type="button"></button></div><div class="b81-setting"><span><b>Animazioni</b><br><small>Transizioni e feedback touch</small></span><button class="b81-toggle on" data-motion type="button"></button></div></div>`;document.body.appendChild(overlay);overlay.querySelector('.b81-close').addEventListener('click',closeOverlay);overlay.addEventListener('click',e=>{if(e.target===overlay)closeOverlay();});overlay.querySelector('[data-compact]').addEventListener('click',e=>{const enabled=!document.body.classList.contains('b81-compact');document.body.classList.toggle('b81-compact',enabled);localStorage.setItem('mp_compact',enabled?'1':'0');e.currentTarget.classList.toggle('on',enabled);toast(enabled?'Vista compatta attiva':'Vista standard attiva');});overlay.querySelector('[data-motion]').addEventListener('click',e=>{const enabled=e.currentTarget.classList.toggle('on');document.documentElement.style.setProperty('--b81-motion',enabled?'1':'0');toast(enabled?'Animazioni attive':'Animazioni ridotte');});}
function expandCard(card){const existing=card.querySelector('.b81-expand');if(existing){existing.remove();card.classList.remove('b81-open');return;}const probabilities=[...card.querySelectorAll('.prob b')].map(n=>n.textContent.trim()),markets=[...card.querySelectorAll('.market-line button')].slice(0,3).map(n=>n.textContent.trim()),panel=document.createElement('div');panel.className='b81-expand';panel.innerHTML=`<div><b>${probabilities[0]||'—'}</b><small>Casa · 1</small></div><div><b>${probabilities[1]||'—'}</b><small>Pareggio · X</small></div><div><b>${probabilities[2]||'—'}</b><small>Ospite · 2</small></div><div class="b81-expand-wide"><b>Segnali verificati</b><small>${markets.join(' · ')||'Dati disponibili nella scheda'}</small></div>`;card.appendChild(panel);card.classList.add('b81-open');toast('Analisi rapida aperta');}
function moveDashboardControls(){const root=appRoot(),hero=root?.querySelector('.hero');if(!hero||hero.dataset.b81Layout==='1')return;hero.dataset.b81Layout='1';const leagueTabs=hero.querySelector('.league-tabs'),viewTabs=hero.querySelector('.view-tabs');if(leagueTabs)hero.after(leagueTabs);if(viewTabs)(leagueTabs||hero).after(viewTabs);root.querySelector('nav')?.classList.add('b81-bottom-nav');}
function updateBuildLabel(){const root=appRoot();if(!root)return;const walker=document.createTreeWalker(root,NodeFilter.SHOW_TEXT),nodes=[];while(walker.nextNode())nodes.push(walker.currentNode);nodes.forEach(node=>{if(node.nodeValue?.includes('BUILD 80'))node.nodeValue=node.nodeValue.replaceAll('BUILD 80','BUILD 85');if(node.nodeValue?.includes('BUILD 81'))node.nodeValue=node.nodeValue.replaceAll('BUILD 81','BUILD 85');if(node.nodeValue?.includes('BUILD 82'))node.nodeValue=node.nodeValue.replaceAll('BUILD 82','BUILD 85');if(node.nodeValue?.includes('BUILD 83'))node.nodeValue=node.nodeValue.replaceAll('BUILD 83','BUILD 85');});}
async function findTeamLogo(name){const key=String(name||'').trim().toLowerCase();if(!key)return null;if(crestCache.has(key))return crestCache.get(key);const promise=(async()=>{try{const r=await fetch(`https://www.sofascore.com/api/v1/search/all?q=${encodeURIComponent(name)}`,{headers:{Accept:'application/json'}});if(!r.ok)return null;const j=await r.json(),rows=j?.results||j?.searchResults||[],hit=rows.find(x=>String(x?.entity?.type||x?.type||'').toLowerCase()==='team'&&x?.entity?.id)||rows.find(x=>x?.entity?.id),id=hit?.entity?.id||hit?.id;if(!id)return null;return `https://api.sofascore.com/api/v1/team/${id}/image`;}catch{return null;}})();crestCache.set(key,promise);return promise;}
async function hydrateCrests(){const root=appRoot();if(!root)return;for(const card of [...root.querySelectorAll('.match-card')])for(const team of card.querySelectorAll('.team')){const box=team.querySelector('.crest');if(!box||box.querySelector('img')||box.dataset.logoLoading==='1')continue;box.dataset.logoLoading='1';const name=team.querySelector('.team-copy b')?.textContent?.trim();if(!name){delete box.dataset.logoLoading;continue;}const src=await findTeamLogo(name);if(src){const img=document.createElement('img');img.alt=`Logo ${name}`;img.loading='lazy';img.src=src;img.onerror=()=>img.remove();box.prepend(img);}delete box.dataset.logoLoading;}}
function enhance(){const root=appRoot();if(!root)return;document.body.classList.add('b82-ui');updateBuildLabel();moveDashboardControls();if(localStorage.getItem('mp_compact')==='1')document.body.classList.add('b81-compact');const searchButton=root.querySelector('.header-icons button:nth-child(1)');if(searchButton&&!searchButton.dataset.b81){searchButton.dataset.b81='1';searchButton.setAttribute('aria-label','Cerca');searchButton.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();openSearch();});}const settingsButton=root.querySelector('.header-icons button:nth-child(3)');if(settingsButton&&!settingsButton.dataset.b81){settingsButton.dataset.b81='1';settingsButton.setAttribute('aria-label','Impostazioni');settingsButton.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();openSettings();});}root.querySelectorAll('.match-card').forEach(card=>{if(card.dataset.b81)return;card.dataset.b81='1';card.setAttribute('role','button');card.setAttribute('aria-expanded','false');card.addEventListener('click',event=>{if(event.target.closest('button,.market-line'))return;expandCard(card);card.setAttribute('aria-expanded',card.classList.contains('b81-open')?'true':'false');});});root.querySelectorAll('.hero-stats div').forEach(stat=>{if(stat.dataset.b81)return;stat.dataset.b81='1';stat.addEventListener('click',()=>{const label=stat.querySelector('small')?.textContent||'Dato',value=stat.querySelector('b')?.textContent||'—';toast(`${label} · ${value}`);});});hydrateCrests();}
const observer=new MutationObserver(()=>requestAnimationFrame(enhance));observer.observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('keydown',event=>{if(event.key==='Escape')closeOverlay();if(event.key==='/'&&!event.ctrlKey&&!event.metaKey&&document.activeElement?.tagName!=='INPUT'){event.preventDefault();openSearch();}});requestAnimationFrame(enhance);
